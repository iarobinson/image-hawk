<% @users.each_with_index do |user, index| %>
  <div class="card">
    <div class="card-body">
      <%= index + 1 %>. <%= user.inspect %><br>
      <%= link_to "View", user_path(user), class: "btn btn-primary float-right" %>
    </div>
  </div>
<% end %>
